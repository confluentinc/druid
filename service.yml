name: druid
lang: unknown
lang_version: unknown
git:
  enable: true
github:
  enable: true
semaphore:
  enable: true
  pipeline_enable: false
  branches:
    - master
    - /^.*-confluent$/
  managed_sections:
    - version
    - name
    - agent
    - global_job_config
